#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: rints the alphabet in lowercase,
 * followed by a new line.
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	char n;

	for (n = 30; n <= 39; ++n)
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}
