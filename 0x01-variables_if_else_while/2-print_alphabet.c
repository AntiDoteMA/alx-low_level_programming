#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: check if n is positive or negativ or zero.
 *
 * Return: Always 0 (Succesful)
*/

int main(void)
{

	char c;

	for (c = 'a'; c <= 'z'; ++c)
	{
		putchar(c);
	}

	putchar('\n');

	return (0);

}
